# Production environment variables for Netlify
# API calls will use relative paths and be proxied to Netlify Functions via netlify.toml redirects

# Leave empty to use relative paths (proxied via Netlify redirects)
VITE_API_URL=
